/**
 * Classes in this package are associated with commands
 * involving the available roles (groups) in the server.
 * These groups are available for joining and leaving,
 * done through the {@link command.commands.roles.Join}
 * and {@link command.commands.roles.Leave} commands
 * and a list of available roles can be viewed with
 * {@link command.commands.roles.Roles Roles}.
 */

package command.commands.roles;
