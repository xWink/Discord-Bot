/**
 * The classes in this package represent the possible commands available
 * to users of the bot. The Command class is an abstraction of commands
 * and all commands must extend it. These commands are stored within the
 * CommandList, which is then accessed to check for which command is
 * meant to be run.
 */

package command;
