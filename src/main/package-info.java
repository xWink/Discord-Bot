/**
 * The classes in this package are used to set up the bot.
 * The setup is based on the settings provided in a config
 * file, which is read in via the Config class.
 *
 * The bot runs with event listeners which are contained in
 * the eventlisteners package.
 *
 * It also utilizes timed tasks contained in the timertasks
 * package.
 */
package main;
